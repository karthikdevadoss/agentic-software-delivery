{
  "_purpose": "Current actionable work discovered during verified sessions — not project history. Resolved items are removed here (their resolution is recorded via the commit that removes them + PROJECT_STATUS.md/LESSONS.md if reusable), not accumulated forever. This is separate from PROJECT_STATE.json's next_phase/next_action, which tracks the one current phase-level priority, not the backlog of smaller items surfaced along the way.",
  "_status_values": ["open", "in_progress", "blocked", "verified", "deferred"],
  "_priority_values": ["critical", "high", "medium", "low"],
  "items": [
    {
      "id": "ACT-001",
      "source": "security",
      "title": "_resolve_safe_path() gives a misleading error for foreign-drive-letter paths (e.g. 'D:foo')",
      "priority": "low",
      "status": "open",
      "scope": "agent/tools.py only",
      "reason": "During V3 correctness review, a path like 'D:foo' (different drive than REPO_ROOT) was correctly rejected but reported as 'symlinks are not allowed', which is factually wrong even though the security boundary holds.",
      "evidence": "Empirical probe during V3 review: tools._resolve_safe_path('D:foo') raised RepoToolError('symlinks are not allowed') instead of a drive/traversal-specific message.",
      "safe_to_auto_execute": false,
      "verification_required": "Re-run the same empirical probe and confirm the new error message accurately describes the rejection reason; confirm the path is still rejected either way.",
      "created_at_commit": "8cd4083",
      "resolved_at_commit": null
    },
    {
      "id": "ACT-002",
      "source": "verification",
      "title": "tools.py's own security boundary has no test file calling it directly (coverage now exists only via downstream consumers)",
      "priority": "low",
      "status": "open",
      "scope": "new test file under agent/ (e.g. test_tools.py), no production code changes",
      "reason": "Originally: zero automated coverage existed. Now substantially mitigated: agent/test_mcp_server.py and agent/test_write_tools.py exercise traversal/absolute/symlink/secret-name rejection through tools._resolve_safe_path indirectly (via MCP and the write boundary), and this indirect coverage caught a real bug (see LESSONS.md: secret-name check skipped for new files). Remaining gap: no test calls tools.read_file/list_repository_files/search_code/_resolve_safe_path directly, and redaction is only verified via rag_index's wrapper, not tools.read_file itself.",
      "evidence": "agent/test_write_tools.py and agent/test_mcp_server.py collectively exercise the shared security boundary through two different consumers; no agent/test_tools.py exists.",
      "safe_to_auto_execute": false,
      "verification_required": "A test file exists calling tools.py functions directly (not through a consumer) for traversal/symlink/absolute-path/redaction, and passes.",
      "created_at_commit": "8cd4083",
      "resolved_at_commit": null
    },
    {
      "id": "ACT-005",
      "source": "architecture",
      "title": "MCP Streamable HTTP transport has never actually been run/tested (only coded/documented)",
      "priority": "low",
      "status": "open",
      "scope": "agent/mcp_server.py only, no production code changes",
      "reason": "mcp.run(transport='streamable-http', ...) is the documented path for a future hosted multi-tenant platform, but only stdio has been exercised so far (via mcp_demo.py's in-process Client). An untested code path could be silently wrong when we actually try to host it.",
      "evidence": "agent/mcp_server.py's docstring documents the streamable-http invocation; no test or run has exercised it in this session.",
      "safe_to_auto_execute": false,
      "verification_required": "Start the server with transport='streamable-http' and confirm a real HTTP-based MCP client (not just in-process) can discover and invoke tools, before this transport is relied on for hosting.",
      "created_at_commit": "d312dd1",
      "resolved_at_commit": null
    },
    {
      "id": "ACT-006",
      "source": "architecture",
      "title": "qa-evaluator custom subagent was not available as a subagent_type in a session that predated its creation",
      "priority": "low",
      "status": "verified",
      "scope": "verification only, no production code changes expected",
      "reason": "During Architecture V2 Shadow Trial #1 (docs/ARCHITECTURE_V2_EVALUATION_PLAN.md), invoking the Agent tool with subagent_type='qa-evaluator' (.claude/agents/qa-evaluator.md, added in the immediately prior task) failed with 'Agent type not found' -- worked around by having a general-purpose subagent read and follow that file's instructions directly. Root cause confirmed (not just suspected): custom subagent definitions load once at session start; both prior sessions (Trial #1 and Trial #2) were long-running sessions that started before/without a reload after the file existed in a state the harness's project-scan picked up. A genuinely fresh Claude Code v2.1.268 session in this repo lists qa-evaluator natively in its available-agent-types system reminder from the first turn.",
      "evidence": "Fresh-session probe (2026-09-11): Agent tool invoked with subagent_type='qa-evaluator' and a trivial read-only task ('report the latest V2 trial number without changing any files'). Result returned as a genuinely separate subagent context: it self-identified in its own reply as 'the qa-evaluator subagent' with the exact prescribed duty from .claude/agents/qa-evaluator.md, used only its allowed tools (Read/Grep, 4 tool_uses, 22186ms), and reported real findings (highest trial = Trial #2) with correct source citations. This is direct evidence of native separate-context execution, not a general-purpose fallback silently answering in the requested persona.",
      "safe_to_auto_execute": false,
      "verification_required": "DONE: native invocation confirmed live in a fresh session; the subagent's own self-reported identity plus tool-call telemetry is the proof.",
      "created_at_commit": "9cbc7dd",
      "resolved_at_commit": "cd58f11"
    },
    {
      "id": "LEARN-WIKI-P0",
      "source": "product",
      "title": "Learn recursive technical Wikipedia -- verified via real browser E2E + independent qa-evaluator",
      "priority": "high",
      "status": "verified",
      "scope": "agent/build_learn_tree.py, agent/learn_tree.py, agent/web/learn.{html,js,css}, agent/web_server.py::learn_page, e2e/learn.spec.js",
      "reason": "P0_PROMPT.txt Sections 3-18: /learn must become a real recursive Wikipedia (domain -> topic -> child topic -> detailed sections), not a flat card dump. A real Playwright browser suite (e2e/learn.spec.js) now exercises the full drill-down (System Design -> Databases -> Connection Pooling -> Pool Sizing), breadcrumbs, browser Back, 404s, and entity/mojibake cleanliness against a real Chromium browser -- passing both locally and against live production. The native qa-evaluator subagent independently re-verified all of this against production itself (running the test suites, not just reading them) and returned PASS.",
      "evidence": "agent/test_learn_tree.py (9/9), agent/test_learn_frontend.js (33/33), agent/test_web_server.py::LearnRecursiveRouteTestCase (6/6), e2e/learn.spec.js (7/7 Playwright, local AND production), independent qa-evaluator PASS verdict (2026-09-11, session task 'Independent QA of Phase 1+2 fixes').",
      "safe_to_auto_execute": false,
      "verification_required": "DONE: real Chromium browser click-through (Playwright) both locally and against production, plus an independent qa-evaluator re-verification running the suites itself.",
      "created_at_commit": "7996b8f",
      "resolved_at_commit": "6ddc628"
    },
    {
      "id": "LEARN-PDF-BOOK-P0",
      "source": "product",
      "title": "Downloadable Learn PDF book generated from the same canonical tree -- verified via real browser E2E + independent qa-evaluator",
      "priority": "high",
      "status": "verified",
      "scope": "agent/learn_pdf.py, agent/web_server.py::get_learn_book_pdf, Learn landing page PDF button, e2e/pdf.spec.js",
      "reason": "P0_PROMPT.txt Sections 37-44: a real, non-screenshot PDF generated from the SAME canonical learn-tree.json. e2e/pdf.spec.js confirms the DOWNLOAD COMPLETE BOOK control is visible in a real browser and that /api/learn/book.pdf returns a real, non-empty PDF (%PDF- signature) -- passing both locally and against production. Independently re-verified by the native qa-evaluator against production (235,663 bytes, %PDF-1.4 signature).",
      "evidence": "agent/test_learn_pdf.py (11/11), agent/test_web_server.py::LearnPdfRouteTestCase (3/3), e2e/pdf.spec.js (2/2 Playwright, local AND production), independent qa-evaluator PASS verdict (2026-09-11).",
      "safe_to_auto_execute": false,
      "verification_required": "DONE: real Chromium browser check (Playwright) that the control is visible and the endpoint returns a genuine PDF, plus independent qa-evaluator re-verification.",
      "created_at_commit": "7996b8f",
      "resolved_at_commit": "6ddc628"
    },
    {
      "id": "SESSION-HISTORY-P0",
      "source": "product",
      "title": "Complete historical Usage session system -- verified via real browser E2E + independent qa-evaluator",
      "priority": "high",
      "status": "verified",
      "scope": "agent/session_history.py, agent/web_server.py (session-history routes), agent/web/usage.{js,css}, e2e/usage.spec.js",
      "reason": "P0_PROMPT.txt Sections 19-23, 30-32: Usage must show ALL captured historical sessions (not just today), grouped by Europe/Berlin date, paginated to the earliest known session, with a dedicated /usage/session/{id} detail route. e2e/usage.spec.js drives a real Chromium browser through the session history list and three real session detail pages (known-cost Workbench run, aggregate-only V2 trial, no-token reconstructed session) -- passing both locally and against production. Building this suite found and fixed a real production defect: usage.html/learn.html referenced their CSS/JS via relative paths, which 404'd (and crashed the page with 'Unexpected token <') on any DIRECT navigation to a nested route -- invisible to client-side pushState navigation and to every prior HTTP-status-only check. Fixed by making all asset references absolute.",
      "evidence": "agent/test_session_history.py (15/15), agent/test_web_server.py::SessionHistoryRouteTestCase (5/5), e2e/usage.spec.js (4/4 Playwright, local AND production), independent qa-evaluator PASS verdict (2026-09-11).",
      "safe_to_auto_execute": false,
      "verification_required": "DONE: real Chromium browser click-through (Playwright) both locally and against production, plus independent qa-evaluator re-verification running the suites itself.",
      "created_at_commit": "7996b8f",
      "resolved_at_commit": "6ddc628"
    },
    {
      "id": "SESSION-ECONOMICS-P0",
      "source": "product",
      "title": "Session-level token/cost economics -- verified via real browser E2E + independent qa-evaluator",
      "priority": "high",
      "status": "verified",
      "scope": "agent/session_history.py's token/cost rendering",
      "reason": "P0_PROMPT.txt Sections 24-26: a session's tokens/cost must be honestly labeled (EXACT/AGGREGATE_ONLY/NOT_CAPTURED for tokens, ACTUAL/COST_UNAVAILABLE-with-reason for cost), and must never contradict the same run's figures shown elsewhere. Real Owner-found defect fixed this task: cost_display_label now gives one of 4 canonical states, and a new Usage-level cost_coverage_summary was added (known cost total, sessions with known/unknown cost, coverage %) -- sourced from the same canonical run_usage_summary events. Verified via a real Chromium browser (Playwright, local + production) confirming a known-cost session shows real tokens/ACTUAL cost, an aggregate-only session shows 'aggregate only' with no fabricated $0, and a not-captured session shows NOT CAPTURED/COST UNAVAILABLE. Independent qa-evaluator re-verified all three states directly against the live API and rendered DOM.",
      "evidence": "agent/test_session_history.py::test_workbench_run_has_exact_tokens_and_actual_cost_when_captured, test_v2_trial_tokens_are_aggregate_only_never_fake_exact, test_cost_display_label_covers_all_four_canonical_states, e2e/usage.spec.js, independent qa-evaluator PASS verdict (2026-09-11).",
      "safe_to_auto_execute": false,
      "verification_required": "DONE: real Chromium browser check of all three cost states plus independent qa-evaluator re-verification against the live API.",
      "created_at_commit": "7996b8f",
      "resolved_at_commit": "6ddc628"
    },
    {
      "id": "SESSION-QUALITY-P0",
      "source": "product",
      "title": "Session value/quality/comparison-to-cohort -- verified via real browser E2E + independent qa-evaluator, Quality now genuinely distinct from Evidence Coverage",
      "priority": "medium",
      "status": "verified",
      "scope": "agent/session_history.py::get_session_detail's value/quality/comparison blocks, agent/web/usage.js",
      "reason": "P0_PROMPT.txt Sections 27-29 plus a real Owner-found defect fixed this task: the top summary previously showed a single conflated 'QUALITY / 100% coverage' figure. quality_score is now honestly None/'NOT SCORED' (this project has no composite quality algorithm), structurally separate everywhere from evidence_coverage_pct (which legitimately differs per session -- verified live: 100%/HIGH_CONFIDENCE for a rich-evidence run, 0%/LOW_COVERAGE for a thin-evidence one). Comparison enriched with wall-time and human-intervention-rate alongside tokens/cost, all gated on >=3 comparable sessions. Verified via a real Chromium browser (Playwright, local + production) confirming 'Quality' and 'Evidence Coverage' render as two visibly separate stats and comparison metrics beyond a bare cohort-size sentence are shown. Independent qa-evaluator re-verified this exact distinction against the live rendered page.",
      "evidence": "agent/test_session_history.py::test_quality_never_reports_100_percent_without_full_evidence, test_quality_score_is_distinct_from_evidence_coverage, test_comparison_requires_at_least_three_comparable_sessions; e2e/usage.spec.js's Quality-vs-Evidence-Coverage assertion (local + production); independent qa-evaluator PASS verdict (2026-09-11).",
      "safe_to_auto_execute": false,
      "verification_required": "DONE: real Chromium browser check confirming Quality and Evidence Coverage render as visibly separate stats, plus independent qa-evaluator re-verification.",
      "created_at_commit": "7996b8f",
      "resolved_at_commit": "6ddc628"
    },
    {
      "id": "PLAYWRIGHT-E2E-P0",
      "source": "product",
      "title": "Browser-level automated regression suite (Playwright) -- established as the project's new quality gate",
      "priority": "high",
      "status": "verified",
      "scope": "package.json, playwright.config.js, e2e/*.spec.js",
      "reason": "Owner directive: 'Owner manual review should verify product quality, not discover basic deterministic defects.' Smallest robust Playwright Test setup added -- resilient role/text/scoped-class locators, built-in auto-waiting/web-first assertions, no arbitrary sleeps. Usable locally (auto-starts the dev server via webServer config), against an already-running server, or as read-only smoke verification against production (PLAYWRIGHT_BASE_URL env var, never touches the local server in that mode). Immediately found a real, serious production defect (relative asset paths breaking every nested Learn/Usage route on direct navigation) that every prior HTTP-status-only verification in this project's history had missed.",
      "evidence": "13/13 tests passing locally and 13/13 against production (2026-09-11); independent qa-evaluator ran this exact suite itself against production as part of its own verification and confirmed the same result.",
      "safe_to_auto_execute": false,
      "verification_required": "DONE.",
      "created_at_commit": "6ddc628",
      "resolved_at_commit": "6ddc628"
    },
    {
      "id": "PRODUCTION-TRANSIENT-502-OBSERVATION",
      "source": "qa-evaluator",
      "title": "PLAUSIBLY EXPLAINED (2026-09-13): a real transient production 502/timeout was observed during independent QA, self-recovered; a matching real mechanism was later found and fixed",
      "priority": "medium",
      "status": "open",
      "scope": "agentic-platform-backend Railway service (production)",
      "reason": "The native qa-evaluator subagent, while independently verifying Phase 1+2 against production (2026-09-11), observed a real HTTP 502 Bad Gateway from Railway's own edge on /learn after a ~300s hang, followed by several minutes of request timeouts, with general internet connectivity confirmed fine throughout. Production self-recovered; an immediate rerun of curl, the Python suite, and the full Playwright suite all passed cleanly. Root cause NOT established -- plausibly an unrelated Railway platform event, OR the burst of ~13 concurrent Playwright browser sessions (used for this task's own verification) straining the backend. This is evidence worth the Owner's attention, not a contract failure (uptime/resilience under concurrent E2E load was not one of the stated acceptance criteria) and NOT something this autonomous task should decide how to address (could imply needing Railway plan changes, backend concurrency limits, or simply monitoring -- all business/infrastructure judgment calls).",
      "evidence": "qa-evaluator's own verification report (2026-09-11, task 'Independent QA of Phase 1+2 fixes') -- observed via direct curl against the real production edge, includes response headers (server: railway-hikari, x-railway-edge: ams1). UPDATE (2026-09-13, WORKBENCH RELIABILITY session): a real, independently-confirmed full-service outage on the SAME platform-backend service was root-caused this task via a direct pg_stat_activity query -- a connection left idle-in-transaction for 963s, holding a lock that blocked ensure_schema()'s DDL, which nearly every request-serving code path calls first; compounded by 4 route handlers running real synchronous DB queries directly on the asyncio event loop, so one stuck query froze every concurrent request. Fixed (commit 4e15656): run_in_threadpool for all 4 handlers, a 15s statement_timeout, and a 30s idle_in_transaction_session_timeout on every event-ledger connection. This is a PLAUSIBLE, not certain, explanation for the 2026-09-11 event specifically (the exact failing query/connection from that date was never captured) -- offered as the most consistent known mechanism, not claimed as definitively proven for that specific historical incident.",
      "safe_to_auto_execute": false,
      "verification_required": "Owner decision, narrowed by the 2026-09-13 fix: with the event loop no longer blockable by one slow DB call and Postgres now self-terminating orphaned idle-in-transaction connections, decide whether the remaining residual risk (e.g. bounding Playwright's local worker concurrency against production, or upgrading the event-ledger Postgres plan) still needs action, or whether this class of failure is now adequately mitigated.",
      "created_at_commit": "6ddc628",
      "resolved_at_commit": null
    },
    {
      "id": "PHASE-3-UPDATE-EMAIL-BACKEND-DEMO",
      "source": "product",
      "title": "READY, NOT STARTED: moderately larger Java/Spring backend requirement (Update Email) proving the platform end-to-end",
      "priority": "medium",
      "status": "deferred",
      "scope": "app/src/main/java/com/example/customer/* (Controller/Service/Repository/validation), app/src/test/java/*, agent/web/index.html (Customer App UI), Learn content generation from the real development chronology",
      "reason": "Explicitly deferred per this task's own instructions ('If Phase 3 cannot safely finish within the current bounded task: finish Phases 1+2 completely, record Phase 3 as READY, and STOP rather than leaving partially deployed backend behavior'). Phases 1 and 2 consumed the full scope safely completable in this task (7 real Owner-found defects fixed and regression-tested, a full Playwright browser suite established and independently verified). Starting a real Customer-App production feature (with its own real API-driven implementation, Java tests, browser E2E, qa-evaluator gate, production deployment, and Learn content generation) deserves a dedicated task with its own full budget, not the tail end of an already-large one.",
      "evidence": "Acceptance contract fully specified in this task's own instructions (Update Email: valid-email/unique/404/409/persisted-on-GET/existing-behavior-unchanged/UI/tests/production-verification); Customer App's current real structure (CustomerController/CustomerService/CustomerRepository/Customer entity, H2, 7 existing tests) already inspected and ready to extend.",
      "safe_to_auto_execute": false,
      "verification_required": "A future task: implement Update Email (preferably executed THROUGH the real Workbench/agentic-delivery pipeline where practical, per the original instruction's Section 12, not hand-coded directly), with Java unit + API/integration tests, browser E2E, native qa-evaluator gate, real deployment, production requested-effect verification, and evidence-backed Learn content generated from the real development chronology.",
      "created_at_commit": "6ddc628",
      "resolved_at_commit": null
    },
    {
      "id": "ACT-007",
      "source": "production_incident",
      "title": "NEEDS_OWNER_DECISION (narrowed): secure git-push architecture now implemented and proven live end-to-end; only the write-credential itself remains an explicit Owner decision",
      "priority": "medium",
      "status": "open",
      "scope": "agent/demo_execution.py (create_isolated_workspace/commit_change/push_change/trigger_deploy/wait_for_new_deployment), agent/web_server.py's _run_trainer_thread/_run_reset_thread",
      "reason": "WORKBENCH RELIABILITY P0 (2026-09-13) redesigned this from scratch after determining the deployed container's own long-lived checkout was never the right thing to mutate/push at all (the original question this action item asked before any fix: 'is this environment supposed to mutate/push a Git checkout, or should it use an isolated workspace instead?' -- answer: isolated workspace). Each run now clones a fresh, disposable temp-dir workspace from the real public GitHub URL (never touches the container's own long-lived checkout), commits to a dedicated `demo/<run_id>` branch (never master), and pushes only if `DEMO_GIT_PUSH_TOKEN` is configured (still NOT provisioned -- an explicit Owner secret decision this task still does not make unilaterally); an unconfigured token is treated as an honest no-op, never a silent failure, and the token is redacted from all captured subprocess output before it can reach any logged/streamed event. `railway link`/`railway up` were separately hardened as non-fatal-link-then-deploy-anyway (see the 4 real production incidents root-caused this same phase, recorded in docs/LESSONS.md), and deployment identity is now proven via real Railway metadata timestamps rather than id-difference guessing. A real end-to-end acceptance run (Playwright, RUN_REAL_ACCEPTANCE=1) exercising the full isolated-workspace -> commit -> push-attempt -> deploy -> timestamp-verified-new-deployment -> production-content-verified -> reset chain passed against live production (2026-09-13), independently re-confirmed via direct curl.",
      "evidence": "agent/test_demo_execution.py's RealLocalGitWorkflowTestCase (uses a real local git repo as origin, not mocks) + DeployTriggerTestCase + DeploymentIdentityTestCase (19 tests, all passing); e2e/workbench-real-acceptance.spec.js passing against real production (2026-09-13, ~2.2 minutes, independently curl-verified footer change + reset-to-baseline).",
      "safe_to_auto_execute": false,
      "verification_required": "Owner decision, narrowed to exactly one open question: provision `DEMO_GIT_PUSH_TOKEN` as a Railway secret scoped to write only the demo branch prefix on this one public repo (least-privilege PAT/deploy key), or explicitly accept that demo-run commits exist only in each run's disposable clone (pushed nowhere, discarded after the run) and that canonical history stays in sync only via a developer/Claude Code session's own push -- either way, the architecture question this item originally asked (isolated workspace vs. mutating the deployed container's own checkout) is now answered and implemented, not still open.",
      "created_at_commit": "cf8186f",
      "resolved_at_commit": null
    },
    {
      "id": "ACT-008",
      "source": "product",
      "title": "RESOLVED IN FOUNDATION (2026-09-13): the backend production-verification mechanism now exists and was proven live end-to-end -- still INTERNAL/GATED, not publicly enabled",
      "priority": "medium",
      "status": "deferred",
      "scope": "agent/backend_catalogue.py, agent/backend_execution.py, agent/backend_acceptance.py (new, internal-only -- no public Workbench route touches any of this)",
      "reason": "The structural gap this item originally identified (no verification mechanism exists that can derive a targeted, deterministic production assertion from a compiled Java/Spring change) is now CLOSED at the foundation level, mirroring demo_catalogue.py's anchor-pattern design applied to one Java string constant (CustomerService.CUSTOMER_NOT_FOUND_MESSAGE, deliberately isolated for exactly this purpose in a separate commit this same phase) instead of static HTML, with a JSON-field extractor (backend_catalogue.extract_value_from_not_found_api_response) instead of a DOM/text anchor. A REAL, LIVE, end-to-end internal acceptance run (agent/backend_acceptance.py) was executed against real production this phase, using a safe, clearly-marked test value, never the actual reserved future target text ('Customer record not found'): real isolated-workspace clone from the real public GitHub repo -> real `mvnw compile` -> real targeted JUnit + the real RestTemplate-based integration test -> real git commit (dedicated branch, never master) -> real git push attempt (honestly NOT_CONFIGURED, ACT-007) -> real Railway deploy -> real deployment-identity confirmation (timestamp-parsed, not raw-string-compared) -> a real live GET /customers/<id> API call independently confirming the exact probe value live in production -> a second full cycle restoring the real baseline, independently reconfirmed live. Three genuine, previously-unknown production-blocking defects were found and fixed getting here (see docs/LESSONS.md and the 3 commits fixing them): app/mvnw was never git-tracked as executable (100644, not 100755); this machine's core.autocrlf=true corrupted app/mvnw's shebang to CRLF on every fresh clone (fixed via .gitattributes, the durable fix -- not a per-machine workaround); and wait_for_new_deployment() compared deployment timestamps as raw strings instead of parsed datetimes, which happened to work for every prior fixture but is not reliably correct in general (fixed, with a constructed counterexample regression test). One honest, not-yet-explained limitation: the acceptance script's own end-to-end run experienced an unexplained polling delay after Railway genuinely reached SUCCESS status (production was independently confirmed correct via direct curl and via a direct, isolated call to the same wait_for_new_deployment() function, which found the real deployment correctly in 10s) -- worth a future investigation, not blocking, and not hidden.",
      "evidence": "agent/test_backend_catalogue.py (21/21), agent/test_backend_execution.py's real-Maven suite (5/5, genuine mvnw compile/test against a real isolated clone of this project's own app/ module), agent/test_demo_execution.py::MvnwCheckoutIntegrityTestCase (3/3, real clone proving the .gitattributes fix), agent/test_dev_check.py (5/5). Live production evidence: direct curl before the run confirmed baseline 'Customer not found: 999999999'; direct curl after the probe cycle confirmed 'Customer record not found (ACT-008 verification probe): 999999999' genuinely live; direct curl after the restore cycle confirmed the exact original baseline restored. Railway deployment history: 8c64f794/1da5acf1/9083bbaf FAILED (the mvnw bugs, root-caused via `railway logs --build`), ff322e24 SUCCESS (probe, live-verified), 8157a6a7 SUCCESS (restore, live-verified).",
      "safe_to_auto_execute": false,
      "verification_required": "NOT PUBLICLY ENABLED -- per this phase's explicit instruction, this remains an INTERNAL BACKEND ACCEPTANCE SCENARIO, not a public Workbench operation, until: repeated execution shows no instability (only one full live cycle has been run), the unexplained script-level polling delay above is understood, and the Owner decides this specific scenario (and its exact target text) is ready to become a public sample. The actual message-text change to 'Customer record not found' remains deliberately NOT applied to the real baseline -- reserved as the real first public demo of this scenario whenever that decision is made.",
      "created_at_commit": "cf8186f",
      "resolved_at_commit": null
    },
    {
      "id": "RAG-MCP-EVALS-P0",
      "source": "product",
      "title": "VERIFIED (internal): minimal, real RAG+embeddings+MCP+Evals vertical slice for backend requirement planning",
      "priority": "medium",
      "status": "verified",
      "scope": "agent/backend_rag_corpus.py, agent/backend_rag_index.py, agent/backend_planning.py, agent/mcp_server.py (new search_project_context tool), agent/tools.py (new search_project_context function), agent/eval_runner.py, agent/evals/*.json, agent/backend_acceptance.py (additive Step 1.5 only), .claude/skills/ai-feature-evaluation/SKILL.md",
      "reason": "Owner's trainer wanted practical, interview-defensible RAG/embeddings/MCP/Evals connected to a real problem, not buzzwords. Built ONE vertical slice: a curated (not whole-repo) corpus of Customer-app source/tests + architecture/incident docs, chunked at logical boundaries (Java members/Markdown headings/JSON catalogue items, not blind fixed windows), embedded with the already-decided fastembed provider (zero new model/key), retrieved via a genuine MCP client->server call (search_project_context, read-only, no path parameter), and fed into a deterministic routing gate (agent/backend_planning.classify_backend_routing) that decides DETERMINISTIC_ONLY/USE_RAG_MCP/REJECT_UNAUTHORIZED on the raw requirement alone, BEFORE any retrieval -- so retrieved content (including an adversarial prompt-injection fixture, Eval 4) structurally cannot reach agent/backend_catalogue.py or agent/risk_policy.py's authorization functions. The LLM impact-analysis step is advisory-only, checked by a deterministic groundedness function (Eval 3) that flags any claimed file not actually retrieved. Kept INTERNAL: wired only into agent/backend_acceptance.py's existing manually-run script as additive printed evidence, agent/web_server.py's public routes untouched.",
      "evidence": "agent/test_backend_rag_index.py (28/28: ingestion/chunking/sub-splitting/retrieval), agent/test_mcp_server.py (26/26 total, 10 new search_project_context contract + read-only-boundary tests, including schema-has-no-path-parameter and no-write-tool-name-exposed checks), agent/test_backend_planning.py (28/28: routing, RAG context contract, groundedness, and SecurityEvalTestCase's structural prompt-injection-inertness proof), agent/test_eval_runner.py (12/12). Eval baseline (python agent/eval_runner.py all): retrieval recall_at_3=1.0, recall_at_5=1.0, mrr=0.903 (12 hand-labeled cases); routing_accuracy=1.0, security_case_accuracy=1.0 (14 hand-labeled cases, all above the recorded initial thresholds in agent/eval_runner.py's THRESHOLDS). With-RAG-vs-without comparison (docs/DECISIONS.md): deterministic grep wins when the exact symbol name is already known (69ms, precise), semantic retrieval wins for natural-language/doc-spanning queries a literal grep barely touches (165ms after model warm-up; ~2s cold-start on first call in a fresh process) -- reported honestly, not cherry-picked.",
      "safe_to_auto_execute": false,
      "verification_required": "DONE for the internal scope: real end-to-end MCP tool call verified (agent/test_mcp_server.py + a live manual invocation), real eval baseline recorded above. NOT YET DONE, deliberately deferred: exposing this on the public Workbench route (needs its own reliability/cost review first, per this task's explicit instruction), a get_run_evidence MCP tool (optional per the task, skipped as not yet needed), an LLM-judge groundedness pass (deterministic check judged sufficient for this slice's scope), and pgvector (Postgres already exists for the event ledger, but the curated corpus is ~80 chunks -- local JSON+numpy remains the right-sized choice, revisit only if the corpus grows substantially).",
      "created_at_commit": "1315dbd",
      "resolved_at_commit": "2acef8d"
    }
  ]
}
