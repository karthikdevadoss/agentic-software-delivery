{
  "_purpose": "Current actionable work discovered during verified sessions — not project history. Resolved items are removed here (their resolution is recorded via the commit that removes them + PROJECT_STATUS.md/LESSONS.md if reusable), not accumulated forever. This is separate from PROJECT_STATE.json's next_phase/next_action, which tracks the one current phase-level priority, not the backlog of smaller items surfaced along the way.",
  "_status_values": ["open", "in_progress", "blocked", "verified", "deferred"],
  "_priority_values": ["critical", "high", "medium", "low"],
  "items": [
    {
      "id": "ACT-001",
      "source": "security",
      "title": "_resolve_safe_path() gives a misleading error for foreign-drive-letter paths (e.g. 'D:foo')",
      "priority": "low",
      "status": "open",
      "scope": "agent/tools.py only",
      "reason": "During V3 correctness review, a path like 'D:foo' (different drive than REPO_ROOT) was correctly rejected but reported as 'symlinks are not allowed', which is factually wrong even though the security boundary holds.",
      "evidence": "Empirical probe during V3 review: tools._resolve_safe_path('D:foo') raised RepoToolError('symlinks are not allowed') instead of a drive/traversal-specific message.",
      "safe_to_auto_execute": false,
      "verification_required": "Re-run the same empirical probe and confirm the new error message accurately describes the rejection reason; confirm the path is still rejected either way.",
      "created_at_commit": "8cd4083",
      "resolved_at_commit": null
    },
    {
      "id": "ACT-002",
      "source": "verification",
      "title": "tools.py's own security boundary has no test file calling it directly (coverage now exists only via downstream consumers)",
      "priority": "low",
      "status": "open",
      "scope": "new test file under agent/ (e.g. test_tools.py), no production code changes",
      "reason": "Originally: zero automated coverage existed. Now substantially mitigated: agent/test_mcp_server.py and agent/test_write_tools.py exercise traversal/absolute/symlink/secret-name rejection through tools._resolve_safe_path indirectly (via MCP and the write boundary), and this indirect coverage caught a real bug (see LESSONS.md: secret-name check skipped for new files). Remaining gap: no test calls tools.read_file/list_repository_files/search_code/_resolve_safe_path directly, and redaction is only verified via rag_index's wrapper, not tools.read_file itself.",
      "evidence": "agent/test_write_tools.py and agent/test_mcp_server.py collectively exercise the shared security boundary through two different consumers; no agent/test_tools.py exists.",
      "safe_to_auto_execute": false,
      "verification_required": "A test file exists calling tools.py functions directly (not through a consumer) for traversal/symlink/absolute-path/redaction, and passes.",
      "created_at_commit": "8cd4083",
      "resolved_at_commit": null
    },
    {
      "id": "ACT-005",
      "source": "architecture",
      "title": "MCP Streamable HTTP transport has never actually been run/tested (only coded/documented)",
      "priority": "low",
      "status": "open",
      "scope": "agent/mcp_server.py only, no production code changes",
      "reason": "mcp.run(transport='streamable-http', ...) is the documented path for a future hosted multi-tenant platform, but only stdio has been exercised so far (via mcp_demo.py's in-process Client). An untested code path could be silently wrong when we actually try to host it.",
      "evidence": "agent/mcp_server.py's docstring documents the streamable-http invocation; no test or run has exercised it in this session.",
      "safe_to_auto_execute": false,
      "verification_required": "Start the server with transport='streamable-http' and confirm a real HTTP-based MCP client (not just in-process) can discover and invoke tools, before this transport is relied on for hosting.",
      "created_at_commit": "d312dd1",
      "resolved_at_commit": null
    },
    {
      "id": "ACT-006",
      "source": "architecture",
      "title": "qa-evaluator custom subagent was not available as a subagent_type in a session that predated its creation",
      "priority": "low",
      "status": "verified",
      "scope": "verification only, no production code changes expected",
      "reason": "During Architecture V2 Shadow Trial #1 (docs/ARCHITECTURE_V2_EVALUATION_PLAN.md), invoking the Agent tool with subagent_type='qa-evaluator' (.claude/agents/qa-evaluator.md, added in the immediately prior task) failed with 'Agent type not found' -- worked around by having a general-purpose subagent read and follow that file's instructions directly. Root cause confirmed (not just suspected): custom subagent definitions load once at session start; both prior sessions (Trial #1 and Trial #2) were long-running sessions that started before/without a reload after the file existed in a state the harness's project-scan picked up. A genuinely fresh Claude Code v2.1.268 session in this repo lists qa-evaluator natively in its available-agent-types system reminder from the first turn.",
      "evidence": "Fresh-session probe (2026-09-11): Agent tool invoked with subagent_type='qa-evaluator' and a trivial read-only task ('report the latest V2 trial number without changing any files'). Result returned as a genuinely separate subagent context: it self-identified in its own reply as 'the qa-evaluator subagent' with the exact prescribed duty from .claude/agents/qa-evaluator.md, used only its allowed tools (Read/Grep, 4 tool_uses, 22186ms), and reported real findings (highest trial = Trial #2) with correct source citations. This is direct evidence of native separate-context execution, not a general-purpose fallback silently answering in the requested persona.",
      "safe_to_auto_execute": false,
      "verification_required": "DONE: native invocation confirmed live in a fresh session; the subagent's own self-reported identity plus tool-call telemetry is the proof.",
      "created_at_commit": "9cbc7dd",
      "resolved_at_commit": "cd58f11"
    },
    {
      "id": "LEARN-WIKI-P0",
      "source": "product",
      "title": "Learn recursive technical Wikipedia -- backend/routing/content complete, real browser click-through not yet performed",
      "priority": "high",
      "status": "in_progress",
      "scope": "agent/build_learn_tree.py, agent/learn_tree.py, agent/web/learn.{html,js,css}, agent/web_server.py::learn_page",
      "reason": "P0_PROMPT.txt Sections 3-18: /learn must become a real recursive Wikipedia (domain -> topic -> child topic -> detailed sections), not a flat card dump. Implemented and independently verified via direct HTTP against the live public deployment: /learn (landing), /learn/system-design, /learn/system-design/databases/connection-pooling all return 200 with real content; /learn/<unknown> genuinely 404s server-side (validated against the canonical tree, not a client-side illusion). A dedicated Node harness (agent/test_learn_frontend.js) exercises the real learn.js source and proves breadcrumbs, child-topic links, related-topic resolution, and the Not-Found state all render correctly.",
      "evidence": "agent/test_learn_tree.py (9/9), agent/test_learn_frontend.js (14/14), agent/test_web_server.py::LearnRecursiveRouteTestCase (6/6), live curl checks against https://agentic-platform-backend-production.up.railway.app/learn/... this task.",
      "safe_to_auto_execute": false,
      "verification_required": "A human (or a genuine browser-automation tool, unavailable in this session) must actually click through Learn in a real browser per P0_PROMPT.txt Section 46's LEARN ACCEPTANCE steps 1-14 before this is marked verified -- HTTP/Node-harness evidence is strong but is not the same claim as a real browser click-through.",
      "created_at_commit": "7996b8f",
      "resolved_at_commit": null
    },
    {
      "id": "LEARN-PDF-BOOK-P0",
      "source": "product",
      "title": "Downloadable Learn PDF book generated from the same canonical tree -- backend complete, real browser download not yet performed",
      "priority": "high",
      "status": "in_progress",
      "scope": "agent/learn_pdf.py, agent/web_server.py::get_learn_book_pdf, Learn landing page PDF button",
      "reason": "P0_PROMPT.txt Sections 37-44: a real, non-screenshot PDF generated from the SAME canonical learn-tree.json. Implemented with reportlab (documented justification in agent/learn_pdf.py's own docstring); independently verified against the live public deployment: /api/learn/book.pdf returns a genuine 88-page, 235KB PDF (application/pdf, correct Content-Disposition) containing real domain/topic hierarchy, WHAT/WHY/HOW/WHEN, development steps, interview sections, and metadata (git commit, generated timestamp, topic counts) -- confirmed via pypdf text extraction, not just a byte-count check.",
      "evidence": "agent/test_learn_pdf.py (11/11), agent/test_web_server.py::LearnPdfRouteTestCase (3/3), live PDF fetched from the production URL this task and independently re-parsed.",
      "safe_to_auto_execute": false,
      "verification_required": "A human must actually click DOWNLOAD COMPLETE BOOK (PDF) on the live public Learn page and confirm a real download happens per P0_PROMPT.txt Section 46 steps 35-45 -- not yet performed this session (no browser tool available).",
      "created_at_commit": "7996b8f",
      "resolved_at_commit": null
    },
    {
      "id": "SESSION-HISTORY-P0",
      "source": "product",
      "title": "Complete historical Usage session system -- backend/pagination/routing complete, real browser click-through not yet performed",
      "priority": "high",
      "status": "in_progress",
      "scope": "agent/session_history.py, agent/web_server.py (session-history routes), agent/web/usage.{js,css}",
      "reason": "P0_PROMPT.txt Sections 19-23, 30-32: Usage must show ALL captured historical sessions (not just today), grouped by Europe/Berlin date, paginated to the earliest known session, with a dedicated /usage/session/{id} detail route. Implemented unifying 3 real session kinds already in the event ledger (Claude Code dev sessions, Workbench runs, V2 trial benchmarks); independently verified this task: 38+ real sessions spanning 3 real calendar dates (2026-09-09 through 2026-09-11), pagination reaches the earliest session with zero duplicates, test-fixture session_ids excluded so the ledger's own test noise never appears as a fabricated session.",
      "evidence": "agent/test_session_history.py (13/13), agent/test_web_server.py::SessionHistoryRouteTestCase (5/5), live curl checks against the production URL this task.",
      "safe_to_auto_execute": false,
      "verification_required": "A human must scroll/Load-More through real Session History on the live public Usage page and open a session detail page per P0_PROMPT.txt Section 46 steps 15-34 -- not yet performed this session (no browser tool available).",
      "created_at_commit": "7996b8f",
      "resolved_at_commit": null
    },
    {
      "id": "SESSION-ECONOMICS-P0",
      "source": "product",
      "title": "Session-level token/cost economics (EXACT/AGGREGATE_ONLY/honest COST UNAVAILABLE) -- implemented and tested",
      "priority": "high",
      "status": "in_progress",
      "scope": "agent/session_history.py's token/cost rendering",
      "reason": "P0_PROMPT.txt Sections 24-26: a session's tokens/cost must be honestly labeled (EXACT/AGGREGATE_ONLY/NOT_CAPTURED for tokens, ACTUAL/COST_UNAVAILABLE-with-reason for cost), and must never contradict the same run's figures shown elsewhere (Workbench/Dashboard/Usage). Verified this task: workbench_run sessions show EXACT tokens + ACTUAL cost from the same event_ledger.get_usage_economics()-adjacent run_usage_summary events already canonical elsewhere; v2_trial_benchmark sessions correctly show AGGREGATE_ONLY tokens and COST_UNAVAILABLE (never a fabricated USD figure from an aggregate total); claude_code_dev_session correctly shows NOT_CAPTURED (Claude Code's hook interface exposes no token field, confirmed by direct code inspection in the prior task).",
      "evidence": "agent/test_session_history.py::test_workbench_run_has_exact_tokens_and_actual_cost_when_captured, test_v2_trial_tokens_are_aggregate_only_never_fake_exact.",
      "safe_to_auto_execute": false,
      "verification_required": "Cross-page economics audit (Workbench/Dashboard/Usage/session detail all agreeing on one canonical run's numbers) should be spot-checked visually once a browser tool is available -- not yet done this session.",
      "created_at_commit": "7996b8f",
      "resolved_at_commit": null
    },
    {
      "id": "SESSION-QUALITY-P0",
      "source": "product",
      "title": "Session value/quality/comparison-to-cohort -- implemented and tested, evidence-gated (never a fake 100%)",
      "priority": "medium",
      "status": "in_progress",
      "scope": "agent/session_history.py::get_session_detail's value/quality/comparison blocks",
      "reason": "P0_PROMPT.txt Sections 27-29: value/quality must be evidence-backed (never fabricated monetary value, never an unqualified 100% score) and a session must be compared against a real cohort only once >=3 comparable sessions exist. Verified this task against a real workbench run (trainer-4733d1c0): technical/learning value computed from real ledger facts, quality score gated by evidence_coverage_pct, comparison correctly requires >=3 comparable same-kind sessions in the last 7 days before computing a median (26 real comparable sessions found, real +17.1% tokens / +13.5% cost vs. cohort median reported).",
      "evidence": "agent/test_session_history.py::test_quality_never_reports_100_percent_without_full_evidence, test_comparison_requires_at_least_three_comparable_sessions; live comparison numbers reproduced this task against the real ledger.",
      "safe_to_auto_execute": false,
      "verification_required": "Visual spot-check of the rendered Quality/Comparison sections on a real session detail page once a browser tool is available.",
      "created_at_commit": "7996b8f",
      "resolved_at_commit": null
    }
  ]
}
